import { Structure as _Structure_ } from "@aws-sdk/types";

export const _ConfigSnapshotDeliveryProperties: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    deliveryFrequency: {
      shape: {
        type: "string"
      }
    }
  }
};
