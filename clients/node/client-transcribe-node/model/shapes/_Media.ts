import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Media: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    MediaFileUri: {
      shape: {
        type: "string",
        min: 1
      }
    }
  }
};
