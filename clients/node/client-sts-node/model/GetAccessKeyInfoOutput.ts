import { Structure as _Structure_ } from "@aws-sdk/types";

export const GetAccessKeyInfoOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Account: {
      shape: {
        type: "string"
      }
    }
  }
};
