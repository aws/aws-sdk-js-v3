import { Structure as _Structure_ } from "@aws-sdk/types";

export const DecodeAuthorizationMessageOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DecodedMessage: {
      shape: {
        type: "string"
      }
    }
  }
};
