import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Statistics: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    count: {
      shape: {
        type: "integer"
      }
    }
  }
};
