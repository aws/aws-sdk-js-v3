import { Structure as _Structure_ } from "@aws-sdk/types";

export const _StatisticalThreshold: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    statistic: {
      shape: {
        type: "string"
      }
    }
  }
};
