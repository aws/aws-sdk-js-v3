import { Structure as _Structure_ } from "@aws-sdk/types";

export const _CodeSigningSignature: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    inlineDocument: {
      shape: {
        type: "blob"
      }
    }
  }
};
