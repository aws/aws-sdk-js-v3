import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Term: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    SourceText: {
      shape: {
        type: "string"
      }
    },
    TargetText: {
      shape: {
        type: "string"
      }
    }
  }
};
