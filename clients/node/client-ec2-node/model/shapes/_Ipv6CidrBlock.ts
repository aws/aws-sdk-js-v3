import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Ipv6CidrBlock: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Ipv6CidrBlock: {
      shape: {
        type: "string"
      },
      locationName: "ipv6CidrBlock"
    }
  }
};
