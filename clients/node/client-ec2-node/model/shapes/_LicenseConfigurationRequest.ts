import { Structure as _Structure_ } from "@aws-sdk/types";

export const _LicenseConfigurationRequest: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    LicenseConfigurationArn: {
      shape: {
        type: "string"
      }
    }
  }
};
