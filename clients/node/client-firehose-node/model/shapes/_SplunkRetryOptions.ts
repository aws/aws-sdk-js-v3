import { Structure as _Structure_ } from "@aws-sdk/types";

export const _SplunkRetryOptions: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DurationInSeconds: {
      shape: {
        type: "integer"
      }
    }
  }
};
