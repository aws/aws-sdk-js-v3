import { Structure as _Structure_ } from "@aws-sdk/types";

export const _BatchArrayProperties: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Size: {
      shape: {
        type: "integer"
      }
    }
  }
};
