import { Structure as _Structure_ } from "@aws-sdk/types";

export const CreateEnvironmentEC2Output: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    environmentId: {
      shape: {
        type: "string"
      }
    }
  }
};
