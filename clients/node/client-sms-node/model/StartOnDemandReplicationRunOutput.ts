import { Structure as _Structure_ } from "@aws-sdk/types";

export const StartOnDemandReplicationRunOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    replicationRunId: {
      shape: {
        type: "string"
      }
    }
  }
};
