import { Structure as _Structure_ } from "@aws-sdk/types";

export const PutApprovalResultOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    approvedAt: {
      shape: {
        type: "timestamp"
      }
    }
  }
};
