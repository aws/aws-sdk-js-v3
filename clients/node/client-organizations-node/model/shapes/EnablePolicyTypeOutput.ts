import { _Root } from "./_Root";
import { Structure as _Structure_ } from "@aws-sdk/types";

export const EnablePolicyTypeOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Root: {
      shape: _Root
    }
  }
};
