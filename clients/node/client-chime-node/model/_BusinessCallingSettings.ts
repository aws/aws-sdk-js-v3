import { Structure as _Structure_ } from "@aws-sdk/types";

export const _BusinessCallingSettings: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    CdrBucket: {
      shape: {
        type: "string"
      }
    }
  }
};
