import { Structure as _Structure_ } from "@aws-sdk/types";

export const _ServiceEndpoint: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Endpoint: {
      shape: {
        type: "string"
      }
    }
  }
};
