import { Structure as _Structure_ } from "@aws-sdk/types";

export const _LabelingJobResourceConfig: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    VolumeKmsKeyId: {
      shape: {
        type: "string"
      }
    }
  }
};
