import { Structure as _Structure_ } from "@aws-sdk/types";

export const TestEventPatternOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Result: {
      shape: {
        type: "boolean"
      }
    }
  }
};
