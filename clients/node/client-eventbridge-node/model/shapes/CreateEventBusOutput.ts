import { Structure as _Structure_ } from "@aws-sdk/types";

export const CreateEventBusOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    EventBusArn: {
      shape: {
        type: "string"
      }
    }
  }
};
