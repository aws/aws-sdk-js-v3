import { Structure as _Structure_ } from "@aws-sdk/types";

export const UpdateSchemaOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    SchemaArn: {
      shape: {
        type: "string"
      }
    }
  }
};
