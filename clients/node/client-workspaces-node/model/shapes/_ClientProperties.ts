import { Structure as _Structure_ } from "@aws-sdk/types";

export const _ClientProperties: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    ReconnectEnabled: {
      shape: {
        type: "string"
      }
    }
  }
};
