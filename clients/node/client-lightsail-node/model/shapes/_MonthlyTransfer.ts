import { Structure as _Structure_ } from "@aws-sdk/types";

export const _MonthlyTransfer: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    gbPerMonthAllocated: {
      shape: {
        type: "integer"
      }
    }
  }
};
