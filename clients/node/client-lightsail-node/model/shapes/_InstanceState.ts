import { Structure as _Structure_ } from "@aws-sdk/types";

export const _InstanceState: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    code: {
      shape: {
        type: "integer"
      }
    },
    name: {
      shape: {
        type: "string"
      }
    }
  }
};
