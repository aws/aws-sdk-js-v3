import { Structure as _Structure_ } from "@aws-sdk/types";

export const _ZoneAwarenessConfig: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    AvailabilityZoneCount: {
      shape: {
        type: "integer"
      }
    }
  }
};
