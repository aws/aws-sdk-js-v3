import { Structure as _Structure_ } from "@aws-sdk/types";

export const _HlsConfiguration: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    ManifestEndpointPrefix: {
      shape: {
        type: "string"
      }
    }
  }
};
