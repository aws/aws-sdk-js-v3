import { Structure as _Structure_ } from "@aws-sdk/types";

export const SendEmailOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    MessageId: {
      shape: {
        type: "string"
      }
    }
  }
};
