import { Structure as _Structure_ } from "@aws-sdk/types";

export const _VideoSelectorPid: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Pid: {
      shape: {
        type: "integer"
      },
      locationName: "pid"
    }
  }
};
