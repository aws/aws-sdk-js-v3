import { Structure as _Structure_ } from "@aws-sdk/types";

export const GetAWSOrganizationsAccessStatusOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    AccessStatus: {
      shape: {
        type: "string"
      }
    }
  }
};
