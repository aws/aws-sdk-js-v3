import { Structure as _Structure_ } from "@aws-sdk/types";

export const GetFileUploadURLOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    FileUploadURL: {
      shape: {
        type: "string"
      }
    }
  }
};
