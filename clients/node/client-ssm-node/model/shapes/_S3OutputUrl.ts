import { Structure as _Structure_ } from "@aws-sdk/types";

export const _S3OutputUrl: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    OutputUrl: {
      shape: {
        type: "string"
      }
    }
  }
};
