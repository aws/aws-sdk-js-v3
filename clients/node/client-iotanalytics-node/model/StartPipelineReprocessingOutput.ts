import { Structure as _Structure_ } from "@aws-sdk/types";

export const StartPipelineReprocessingOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    reprocessingId: {
      shape: {
        type: "string"
      }
    }
  }
};
