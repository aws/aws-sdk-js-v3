import { _PropertyGroups } from "./_PropertyGroups";
import { Structure as _Structure_ } from "@aws-sdk/types";

export const _EnvironmentPropertyDescriptions: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    PropertyGroupDescriptions: {
      shape: _PropertyGroups
    }
  }
};
