import { Structure as _Structure_ } from "@aws-sdk/types";

export const _EndpointDetails: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    VpcEndpointId: {
      shape: {
        type: "string"
      }
    }
  }
};
