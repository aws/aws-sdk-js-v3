import { Structure as _Structure_ } from "@aws-sdk/types";

export const DeleteSubnetGroupOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DeletionMessage: {
      shape: {
        type: "string"
      }
    }
  }
};
