import { _ArrayValueList } from "./_ArrayValueList";
import { Structure as _Structure_ } from "@aws-sdk/types";

export const _StructValue: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    attributes: {
      shape: _ArrayValueList
    }
  }
};
