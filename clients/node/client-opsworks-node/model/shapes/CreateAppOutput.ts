import { Structure as _Structure_ } from "@aws-sdk/types";

export const CreateAppOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    AppId: {
      shape: {
        type: "string"
      }
    }
  }
};
