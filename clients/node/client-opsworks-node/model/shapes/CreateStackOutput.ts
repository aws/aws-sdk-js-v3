import { Structure as _Structure_ } from "@aws-sdk/types";

export const CreateStackOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    StackId: {
      shape: {
        type: "string"
      }
    }
  }
};
