import { Structure as _Structure_ } from "@aws-sdk/types";

export const CreateCaseOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    caseId: {
      shape: {
        type: "string"
      }
    }
  }
};
