import { Structure as _Structure_ } from "@aws-sdk/types";

export const ResolveCaseInput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    caseId: {
      shape: {
        type: "string"
      }
    }
  }
};
