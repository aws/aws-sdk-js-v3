import { Structure as _Structure_ } from "@aws-sdk/types";

export const _FunctionRunAsConfig: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Gid: {
      shape: {
        type: "integer"
      }
    },
    Uid: {
      shape: {
        type: "integer"
      }
    }
  }
};
