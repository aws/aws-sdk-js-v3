import { Structure as _Structure_ } from "@aws-sdk/types";

export const _DocumentMetadata: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Pages: {
      shape: {
        type: "integer"
      }
    }
  }
};
