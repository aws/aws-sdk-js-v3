import { Structure as _Structure_ } from "@aws-sdk/types";

export const _VideoSelectorProgramId: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    ProgramId: {
      shape: {
        type: "integer"
      },
      locationName: "programId"
    }
  }
};
