import { Structure as _Structure_ } from "@aws-sdk/types";

export const _BatchRetryStrategy: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Attempts: {
      shape: {
        type: "integer"
      }
    }
  }
};
