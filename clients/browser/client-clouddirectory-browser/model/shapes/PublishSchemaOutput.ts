import { Structure as _Structure_ } from "@aws-sdk/types";

export const PublishSchemaOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    PublishedSchemaArn: {
      shape: {
        type: "string"
      }
    }
  }
};
