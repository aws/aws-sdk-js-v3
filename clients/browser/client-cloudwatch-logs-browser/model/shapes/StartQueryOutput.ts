import { Structure as _Structure_ } from "@aws-sdk/types";

export const StartQueryOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    queryId: {
      shape: {
        type: "string"
      }
    }
  }
};
