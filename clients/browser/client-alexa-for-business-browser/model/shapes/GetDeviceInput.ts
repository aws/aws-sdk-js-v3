import { Structure as _Structure_ } from "@aws-sdk/types";

export const GetDeviceInput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DeviceArn: {
      shape: {
        type: "string"
      }
    }
  }
};
