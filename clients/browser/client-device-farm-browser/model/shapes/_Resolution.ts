import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Resolution: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    width: {
      shape: {
        type: "integer"
      }
    },
    height: {
      shape: {
        type: "integer"
      }
    }
  }
};
