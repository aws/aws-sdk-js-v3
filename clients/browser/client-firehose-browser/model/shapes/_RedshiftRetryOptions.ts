import { Structure as _Structure_ } from "@aws-sdk/types";

export const _RedshiftRetryOptions: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DurationInSeconds: {
      shape: {
        type: "integer"
      }
    }
  }
};
