import { Structure as _Structure_ } from "@aws-sdk/types";

export const CreateTrustOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    TrustId: {
      shape: {
        type: "string"
      }
    }
  }
};
