import { Structure as _Structure_ } from "@aws-sdk/types";

export const _ExecutionProperty: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    MaxConcurrentRuns: {
      shape: {
        type: "integer"
      }
    }
  }
};
