import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Identity: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    PrincipalId: {
      shape: {
        type: "string"
      }
    },
    Type: {
      shape: {
        type: "string"
      }
    }
  }
};
