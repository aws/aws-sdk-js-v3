import { Structure as _Structure_ } from "@aws-sdk/types";

export const _RouteFilterPrefix: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    cidr: {
      shape: {
        type: "string"
      }
    }
  }
};
