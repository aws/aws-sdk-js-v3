import { Structure as _Structure_ } from "@aws-sdk/types";

export const RetryStageExecutionOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    pipelineExecutionId: {
      shape: {
        type: "string"
      }
    }
  }
};
