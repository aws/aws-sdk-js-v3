import { Structure as _Structure_ } from "@aws-sdk/types";

export const GetDataEndpointOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DataEndpoint: {
      shape: {
        type: "string"
      }
    }
  }
};
