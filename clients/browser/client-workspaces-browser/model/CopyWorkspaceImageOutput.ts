import { Structure as _Structure_ } from "@aws-sdk/types";

export const CopyWorkspaceImageOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    ImageId: {
      shape: {
        type: "string"
      }
    }
  }
};
