import { Structure as _Structure_ } from "@aws-sdk/types";

export const _StartRequest: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    WorkspaceId: {
      shape: {
        type: "string"
      }
    }
  }
};
