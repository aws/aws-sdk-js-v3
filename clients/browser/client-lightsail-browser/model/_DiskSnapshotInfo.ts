import { Structure as _Structure_ } from "@aws-sdk/types";

export const _DiskSnapshotInfo: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    sizeInGb: {
      shape: {
        type: "integer"
      }
    }
  }
};
