import { List as _List_ } from "@aws-sdk/types";
import { _PropertyGroup } from "./_PropertyGroup";

export const _PropertyGroups: _List_ = {
  type: "list",
  member: {
    shape: _PropertyGroup
  }
};
