import { Structure as _Structure_ } from "@aws-sdk/types";

export const _AuditCheckConfiguration: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    enabled: {
      shape: {
        type: "boolean"
      }
    }
  }
};
