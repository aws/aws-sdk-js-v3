import { Structure as _Structure_ } from "@aws-sdk/types";

export const _SuggestStatus: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    timems: {
      shape: {
        type: "integer"
      }
    },
    rid: {
      shape: {
        type: "string"
      }
    }
  }
};
