import { Structure as _Structure_ } from "@aws-sdk/types";

export const _CidrBlock: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    CidrBlock: {
      shape: {
        type: "string"
      },
      locationName: "cidrBlock"
    }
  }
};
