import { Structure as _Structure_ } from "@aws-sdk/types";

export const EnableEbsEncryptionByDefaultInput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DryRun: {
      shape: {
        type: "boolean"
      }
    }
  }
};
