import { Structure as _Structure_ } from "@aws-sdk/types";

export const _ArrayProperties: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    size: {
      shape: {
        type: "integer"
      }
    }
  }
};
