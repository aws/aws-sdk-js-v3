import { Structure as _Structure_ } from "@aws-sdk/types";

export const _RetryStrategy: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    attempts: {
      shape: {
        type: "integer"
      }
    }
  }
};
