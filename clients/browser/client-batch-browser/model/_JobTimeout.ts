import { Structure as _Structure_ } from "@aws-sdk/types";

export const _JobTimeout: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    attemptDurationSeconds: {
      shape: {
        type: "integer"
      }
    }
  }
};
