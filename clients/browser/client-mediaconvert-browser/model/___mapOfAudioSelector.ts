import { Map as _Map_ } from "@aws-sdk/types";
import { _AudioSelector } from "./_AudioSelector";

export const ___mapOfAudioSelector: _Map_ = {
  type: "map",
  key: {
    shape: {
      type: "string"
    }
  },
  value: {
    shape: _AudioSelector
  }
};
