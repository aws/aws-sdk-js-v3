import { List as _List_ } from "@aws-sdk/types";
import { _OutputChannelMapping } from "./_OutputChannelMapping";

export const ___listOfOutputChannelMapping: _List_ = {
  type: "list",
  member: {
    shape: _OutputChannelMapping
  }
};
