import { Structure as _Structure_ } from "@aws-sdk/types";

export const _NoiseReducerFilterSettings: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Strength: {
      shape: {
        type: "integer"
      },
      locationName: "strength"
    }
  }
};
