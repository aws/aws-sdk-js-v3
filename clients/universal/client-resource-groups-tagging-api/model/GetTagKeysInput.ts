import { Structure as _Structure_ } from "@aws-sdk/types";

export const GetTagKeysInput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    PaginationToken: {
      shape: {
        type: "string"
      }
    }
  }
};
