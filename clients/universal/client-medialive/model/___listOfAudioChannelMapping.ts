import { List as _List_ } from "@aws-sdk/types";
import { _AudioChannelMapping } from "./_AudioChannelMapping";

export const ___listOfAudioChannelMapping: _List_ = {
  type: "list",
  member: {
    shape: _AudioChannelMapping
  }
};
