import { Structure as _Structure_ } from "@aws-sdk/types";

export const _SnapshotOptions: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    AutomatedSnapshotStartHour: {
      shape: {
        type: "integer"
      }
    }
  }
};
