import { Structure as _Structure_ } from "@aws-sdk/types";

export const CloneStackOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    StackId: {
      shape: {
        type: "string"
      }
    }
  }
};
