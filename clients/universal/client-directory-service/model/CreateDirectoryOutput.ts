import { Structure as _Structure_ } from "@aws-sdk/types";

export const CreateDirectoryOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    DirectoryId: {
      shape: {
        type: "string"
      }
    }
  }
};
