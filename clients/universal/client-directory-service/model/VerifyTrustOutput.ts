import { Structure as _Structure_ } from "@aws-sdk/types";

export const VerifyTrustOutput: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    TrustId: {
      shape: {
        type: "string"
      }
    }
  }
};
