import { Structure as _Structure_ } from "@aws-sdk/types";

export const _ReplicaDescription: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    RegionName: {
      shape: {
        type: "string"
      }
    }
  }
};
