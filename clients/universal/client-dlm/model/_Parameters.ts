import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Parameters: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    ExcludeBootVolume: {
      shape: {
        type: "boolean"
      }
    }
  }
};
