import { Structure as _Structure_ } from "@aws-sdk/types";

export const _Configuration: _Structure_ = {
  type: "structure",
  required: [],
  members: {
    Enabled: {
      shape: {
        type: "boolean"
      }
    }
  }
};
