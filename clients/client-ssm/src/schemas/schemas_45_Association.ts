// smithy-typescript generated code
import { list, map } from "@smithy/core/schema";

import { _TM, _TMa, n0 } from "./schemas_0";

/* eslint no-var: 0 */

export var TargetMaps = list(n0, _TM, 0, map(n0, _TMa, 0, 0, 64 | 0));
export var TargetMapValueList = 64 | 0;

export var TargetMap = map(n0, _TMa, 0, 0, 64 | 0);
