import { v4 } from "uuid";

import { StringType } from "../item";
import { hashKey } from "./hashKey";

export function autoGeneratedHashKey(parameters: Partial<StringType> = {}): PropertyDecorator {
  return hashKey({
    ...parameters,
    type: "String",
    defaultProvider: v4,
  });
}
