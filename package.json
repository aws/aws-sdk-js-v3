{
  "name": "aws-sdk-js-next",
  "private": true,
  "version": "0.0.1",
  "description": "AWS SDK for JavaScript from the future",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "pretest": "lerna run pretest",
    "test": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:aws/aws-sdk-js-staging.git"
  },
  "author": "aws-javascript-sdk-team@amazon.com",
  "license": "UNLICENSED",
  "dependencies": {
    "lerna": "^2.0.0",
    "jest": "^20.0.4"
  },
  "jest": {
    "mapCoverage": true,
    "projects": [
      "<rootDir>/packages/*"
    ]
  }
}
