import { describe, test as it } from "vitest";

import { s3ExpressMiddleware } from "./s3ExpressMiddleware";

describe(s3ExpressMiddleware.name, () => {
  it("decides whether to use s3Express auth from the resolved endpoint", async () => {
    //
  });
});
