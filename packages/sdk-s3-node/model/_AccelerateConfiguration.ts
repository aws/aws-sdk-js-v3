import {Structure as _Structure_} from '@aws/types';

export const _AccelerateConfiguration: _Structure_ = {
    type: 'structure',
    required: [],
    members: {
        Status: {
            shape: {
                type: 'string',
            },
        },
    },
};