import { FetchHttpHandler } from "./fetch-http-handler";

describe("fetchHttpHandler", () => {
  it("returns does something when something", done => {
    expect(1).toBe(1);
    // const expected = new Uint8Array(0);

    // const dataPromise = new Response(expected.buffer)
    //   .blob()
    //   .then(blob => FetchHttpHandler(blob));

    // dataPromise.then((data: any) => {
    //   expect(data).toEqual(expected);
    //   done();
    // });
  });
});
